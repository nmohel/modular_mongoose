<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Dash</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <h1>All Sneks</h1>
        <% for (s of all_snakes) { %>
            <div style="border-bottom: 1px solid black; margin-bottom: 10px;">
                <h3>Species: <%= s.species %></h3>
                <h3>Color: <%= s.color %></h3>

                <a href="/snakes/<%= s._id %>">Details</a>
            </div>
        <% } %>
        <button><a href="/snakes/new" style="text-decoration: none; color: black">Add a snake</a></button>
    </body>
</html>